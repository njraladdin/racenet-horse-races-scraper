<h3 class="mb-3">Horse Racing Sectional Data</h3>

<!-- Daily Scraper Section -->
<div class="card mb-4">
  <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
    <h5 class="mb-0">Daily Scraper Data</h5>
    <a href="/download-all/daily" class="btn btn-sm btn-light">Download All Daily Data</a>
  </div>
  <div class="card-body">
    {{#if dailyDates.length}}
      <div class="row">
        <!-- Dates sidebar -->
        <div class="col-md-3 mb-3">
          <label for="daily-dates"><strong>Available Dates:</strong></label>
          <div class="list-group date-list" id="daily-dates">
            {{#each dailyDates}}
              <a href="#" class="list-group-item list-group-item-action daily-date date-selector {{#if (eq this ../latestDailyDate)}}active{{/if}}" 
                 data-type="daily" 
                 data-date="{{this}}">
                {{this}}
              </a>
            {{/each}}
          </div>
        </div>
        <!-- Meetings table -->
        <div class="col-md-9">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <label id="daily-heading"><strong>Meetings for {{latestDailyDate}}:</strong></label>
            {{#if latestDailyDate}}
              <a href="/download-day/daily/{{latestDailyDate}}" class="btn btn-sm btn-primary">Download Day as ZIP</a>
            {{/if}}
          </div>
          <div id="daily-meetings">
            {{#if latestDailyMeetings.length}}
              <div class="table-responsive">
                <table class="table table-striped">
                  <thead>
                    <tr>
                      <th>Meeting</th>
                      <th>Last Updated</th>
                      <th>Size</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    {{#each latestDailyMeetings}}
                      <tr>
                        <td>{{this.name}}</td>
                        <td>{{formatDate this.lastModified}}</td>
                        <td>{{formatFileSize this.size}}</td>
                        <td>
                          <a href="/download/daily/{{../latestDailyDate}}/{{this.name}}" class="btn btn-sm btn-primary">Download</a>
                        </td>
                      </tr>
                    {{/each}}
                  </tbody>
                </table>
              </div>
            {{else}}
              <div class="alert alert-info">No meetings available for this date.</div>
            {{/if}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="alert alert-info">No data has been scraped yet.</div>
    {{/if}}
  </div>
</div>

<!-- Weekly Scraper Section -->
<div class="card mb-4">
  <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
    <h5 class="mb-0">Historical Data</h5>
    <a href="/download-all/weekly" class="btn btn-sm btn-light">Download All Historical Data</a>
  </div>
  <div class="card-body">
    {{#if weeklyDates.length}}
      <div class="row">
        <!-- Dates sidebar -->
        <div class="col-md-3 mb-3">
          <label for="weekly-dates"><strong>Available Dates:</strong></label>
          <div class="list-group date-list" id="weekly-dates">
            {{#each weeklyDates}}
              <a href="#" class="list-group-item list-group-item-action weekly-date date-selector {{#if (eq this ../latestWeeklyDate)}}active{{/if}}" 
                 data-type="weekly" 
                 data-date="{{this}}">
                {{this}}
              </a>
            {{/each}}
          </div>
        </div>
        <!-- Meetings table -->
        <div class="col-md-9">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <label id="weekly-heading"><strong>Meetings for {{latestWeeklyDate}}:</strong></label>
            {{#if latestWeeklyDate}}
              <a href="/download-day/weekly/{{latestWeeklyDate}}" class="btn btn-sm btn-success">Download Day as ZIP</a>
            {{/if}}
          </div>
          <div id="weekly-meetings">
            {{#if latestWeeklyMeetings.length}}
              <div class="table-responsive">
                <table class="table table-striped">
                  <thead>
                    <tr>
                      <th>Meeting</th>
                      <th>Last Updated</th>
                      <th>Size</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    {{#each latestWeeklyMeetings}}
                      <tr>
                        <td>{{this.name}}</td>
                        <td>{{formatDate this.lastModified}}</td>
                        <td>{{formatFileSize this.size}}</td>
                        <td>
                          <a href="/download/weekly/{{../latestWeeklyDate}}/{{this.name}}" class="btn btn-sm btn-success">Download</a>
                        </td>
                      </tr>
                    {{/each}}
                  </tbody>
                </table>
              </div>
            {{else}}
              <div class="alert alert-info">No meetings available for this date.</div>
            {{/if}}
          </div>
        </div>
      </div>
    {{else}}
      <div class="alert alert-info">No data has been scraped yet.</div>
    {{/if}}
  </div>
</div> 